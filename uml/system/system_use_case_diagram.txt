@startuml
!theme plain
left to right direction

actor "ML Developer" as Dev

rectangle "Fraud Detection Pipeline" {
  usecase "Подготовить данные\n(Preparator)" as UC_Prepare
  usecase "Применить модель\n(Processor)" as UC_Process
  usecase "Интерпретировать\n(Interpreter)" as UC_Interpret
  usecase "Обучить модель\n(Trainer)" as UC_Train
}

' Связь с интерфейсами фреймворка
note top of UC_Prepare
  <<implements>>
  Preparator.prepare()
end note

note top of UC_Process
  <<implements>>
  Processor.process()
end note

note top of UC_Interpret
  <<implements>>
  Interpreter.interpret()
end note

note top of UC_Train
  <<implements>>
  Trainer.train()
end note

Dev --> UC_Prepare
Dev --> UC_Process
Dev --> UC_Interpret
Dev --> UC_Train
@enduml